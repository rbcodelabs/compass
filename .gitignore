# dependencies
node_modules
.pnp
.pnp.js

# testing
coverage

# next.js
.next/
out/
build/

# production
dist

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files — ignore ALL of them except the committed example
# (.env.production etc. contain live secrets pulled via `vercel env pull`)
.env
.env.*
!.env.example

# local MCP config — contains a live bearer token; commit a .mcp.json.example instead
.mcp.json

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# prisma
prisma/generated/

# playwright auth state (never commit session cookies)
e2e/.auth/
e2e/functional/.auth/
test-results/
# Phase 0 performance baseline: raw samples, traces, logs, and auth are local-only.
e2e/performance/.auth/
test-results/performance-baseline/
.performance-baseline/
